# DESIGN.md — Shawarma Lu

Записано с построенной страницы, а не с намерения. Меняете систему — правьте
здесь же.

## Тезис

Страница — это прайс-борд у мангала, а не «сайт кафе». Угольная темнота цеха,
и один кремовый разворот бумаги посередине, на котором напечатано меню.
Тёмное → светлое → тёмное. Отказ от главного шаблона тёмных лендингов —
тёмных насквозь и оттого плоских.

## Цвет

Ровно три. Четвёртого нет: всё служебное — прозрачности кости или угля.

| Токен | Значение | Где живёт |
|---|---|---|
| `--color-coal` | `#0C0B0A` | фон ~70% страницы; текст на бумаге |
| `--color-bone` | `#E9E0D2` | текст на угле; разворот меню; две карточки отзывов; надпись в футере |
| `--color-ember` | `#E4531B` | бегущая строка, плашка счётчиков, активный фильтр, кнопки, курсор, маркер, номера кадров |

Контраст: кость на угле **14.9:1**, жар на угле **5.2:1**, жар на кости
**2.9:1**.

**Правило, которое нельзя нарушать:** на кремовых поверхностях жаром нельзя
набирать текст — только заливки и крупная графика. Цены и названия там
набираются углём.

Жар появляется в масштабе страницы дважды: тонкая горячая полоса сразу после
героя и высокая плашка счётчиков. Между ними — бумага. Это и есть ритм.

Свечения нет. `box-shadow` с нулевым смещением и цветным ореолом в этой
системе запрещён; тень бывает только у поднятой бумажной карточки отзыва —
со смещением и размытием.

## Типографика

- **Geologica** — заголовки, цены, счётчики, номера. Вес 700–800, трекинг
  `-0.03…-0.05em`, интерлиньяж `0.82–0.9`. Инженерный резаный гротеск.
- **Golos Text** — весь набор. 400–600, 1.45–1.6.
- Третьей гарнитуры нет. Лейблы — тот же Golos, 11px, `0.2em`, верхний регистр
  (утилита `label`).
- Все цифры табличные (`tnum`).

Шкала живёт в `@theme` как `--text-mega / h1 / h2 / h3 / num`, все на `clamp()`.

## Форма

- **Скруглений нет нигде.** Единственные круги — маркер на карте, точка курсора
  и индикатор «открыто». Прямой угол = печатный ценник.
- Разделители — волосяные линии `bone/14` (на бумаге `coal/14…22`).
- Ромб (квадрат под 45°) — единственный декоративный знак, разделитель в
  бегущей строке и в подписи отзыва.
- Иконки рисованные (SVG, обводка 1.6). Юникод-глифов вместо иконок нет.

## Материалы

- `paper` — кремовый фон с волокном и лёгким затенением сверху слева.
- `soot-layer` — угольное зерно поверх тёмных сцен.
- `leader` — точки-выноска между строкой и значением. Живёт только там, где
  строка достаточно широкая: в часах работы. В узкой сетке меню её нет.

## Движение

Один авторский приём, а не набор эффектов: **всё выезжает снизу с
экспоненциальным замедлением** (`expo.out`, `cubic-bezier(0.16, 1, 0.3, 1)`).
Длительности 0.55–1.8 с.

- Скролл ведёт Lenis, он же обновляет ScrollTrigger.
- Пришпиленная лента «как готовим» — только `md+` и только при разрешённой
  анимации; иначе тот же ряд превращается в обычный горизонтальный скролл.
- Курсор и магнитные кнопки — только `hover: hover` и `pointer: fine`.
- Название в герое проявляется посимвольно на чистом CSS, без JS.

**Контракт:** содержимое видно по умолчанию. Скрытое состояние ставит только
JS в `useEffect`. Поэтому без JS и при `prefers-reduced-motion: reduce`
страница просто статична, а не пуста.

## Поверхности браузера

Выделение текста, скроллбар, фокус-кольца и элементы управления картой
перекрашены в палитру. На бумаге выделение и фокус инвертируются — за это
отвечает класс `on-paper` на секции.

## Сетка

- Боковые поля — одна переменная `--rail: clamp(1.25rem, 5vw, 6rem)`,
  утилита `rail`. Никаких центрированных `max-w` контейнеров.
- Вертикальный ритм секций — `py-[clamp(4rem, 9vh, 8rem)]`.
- Меню: 4 колонки на `lg`, 2 на `sm`, а на телефоне — не сетка вовсе, а список
  с миниатюрами.

## Что здесь считается ошибкой

Градиентный текст, стеклянные карточки, цветные ореолы, карусель отзывов, три
иконки в ряд, скруглённые кнопки, эмодзи вместо иконок, четвёртый цвет,
надстрочный «кикер» над заголовком.
